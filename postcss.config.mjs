const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
    theme: {
    extend: {
      fontFamily: {
        sans: ['var(--font-poppins)', 'sans-serif'], // Yeh line add karein
      },
    },
  },
};

export default config;
